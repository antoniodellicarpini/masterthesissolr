#**
 *  Check if Clustering is Enabled and then
 *  call cluster_results.vm
 *#

<h2 #annTitle("Clusters generated by Carrot2 using the /clustering RequestHandler")>
  Hierarchy
</h2>

## Div tag has placeholder text by default
<div id="hierarchy">
 intestazione Gerarchia
 <p></p>
</div>


<ol>
#foreach ($gerarchie in $response.response.clusterGerarchizzati)
  
   
      <li>
       #if($response.response.responseHeader.get('params').size()==0)
        <a href="#url_for_home?q=*:">
           $gerarchie.get('label')
 		</a>
 		#else
 		<a href="#url_for_home?q=$params.get('q')">
           $gerarchie.get('label')
 		</a>
 		#end
      </li>
  

#end
</ol>   

## end for each Cluster

